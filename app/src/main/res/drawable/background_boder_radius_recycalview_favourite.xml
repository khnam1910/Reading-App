<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#80CAF4FF"/> <!-- Background color (transparent) -->
    <corners android:radius="20dp"/> <!-- Making the shape circular -->
    <stroke
        android:width="2dp"
        android:color="@android:color/transparent"/> <!-- White border color -->
</shape>